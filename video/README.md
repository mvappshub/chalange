# Video artifacts

- Render command: `npm run video:render`
- Output: `video/out/demo.mp4` (if ffmpeg exists) and always `video/out/storyboard.json`

If ffmpeg is not available, script still produces deterministic storyboard manifest.
