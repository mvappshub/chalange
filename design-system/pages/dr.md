# DR Page Overrides

## Purpose
Safe backup/restore flow with explicit visual separation of risk.

## Overrides
- Export action styled as safe primary action.
- Restore section is a dedicated danger-zone card.
- Warning copy must be high-contrast and impossible to miss.
- Restore confirmation stays explicit via browser confirm dialog.
- Success feedback after restore appears as green callout with audit link.
