# Audit Page Overrides

## Purpose
Readable audit stream for operational and agent-generated events.

## Overrides
- Table spacing optimized for dense event logs.
- `agent_ran` summaries shown with highlighted pill style.
- Entity column uses monospace for type:id readability.
- JSON payloads use `<details>` + scrollable code block.
- Preserve table structure while improving visual hierarchy.
